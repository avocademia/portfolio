export type project = {
    name: string,
    role: string,
    description: string,
    key_features: string[],
    stack: string[],
    launch_date: string,
    client: string,
    server: string,
    website: string|undefined,
    photos: string[]
}